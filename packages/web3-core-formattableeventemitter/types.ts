import { ValidTypesEnum } from 'web3-utils/types';

export interface FormattableEventEmitterOptions {
    returnType?: ValidTypesEnum;
}

export interface OnOptions {
    returnType?: ValidTypesEnum;
}
